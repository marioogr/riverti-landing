---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <!-- Navigation -->
  <nav class="nav">
    <div class="nav-container">
      <div class="nav-logo">
        <span class="logo-text">River<span class="logo-accent">TI</span></span>
      </div>
      <div class="nav-menu">
        <a href="#inicio" class="nav-link">Inicio</a>
        <a href="#servicios" class="nav-link">Servicios</a>
        <a href="#productos" class="nav-link">Productos</a>
        <a href="#nosotros" class="nav-link">Nosotros</a>
        <a href="#contacto" class="nav-link">Contacto</a>
      </div>
      <div class="nav-toggle">
        <span></span>
        <span></span>
        <span></span>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section id="inicio" class="hero">
    <div class="hero-background">
      <div class="grid-overlay"></div>
    </div>
    <div class="hero-container">
      <div class="hero-content">
        <h1 class="hero-title">
          Transformamos tu negocio con
          <span class="gradient-text">tecnología inteligente</span>
        </h1>
        <p class="hero-subtitle">
          Automatización, software a medida, data science y consultoría de TI.
        </p>
        <div class="hero-cta">
          <a href="#contacto" class="btn btn-primary">Comenzar Proyecto</a>
          <a href="#servicios" class="btn btn-secondary">Ver Servicios</a>
        </div>
      </div>
      <div class="hero-visual">
        <div class="gradient-orb"></div>
        <div class="gradient-mesh"></div>
      </div>
    </div>
  </section>

  <!-- Productos / SAAS Section -->
  <section id="productos" class="products">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">Nuestros Productos</h2>
        <p class="section-subtitle">
          Soluciones SAAS innovadoras para potenciar tu negocio
        </p>
      </div>
      <div class="products-grid">
        <div class="product-card">
          <div class="product-icon">
            <div class="icon-bg">🚗</div>
          </div>
          <h3 class="product-title">RiverTI Car</h3>
          <p class="product-description">
            Plataforma de gestión para automotoras potenciada por IA. Optimiza
            la administración de vehículos, clientes y ventas con la ayuda de
            RiverTI Bot.
          </p>
          <ul class="product-features">
            <li>Gestión de proyectos y tareas</li>
            <li>Colaboración en tiempo real</li>
            <li>Informes y análisis de rendimiento</li>
            <li>Integraciones con otras herramientas</li>
          </ul>
        </div>
        <div class="product-card">
          <div class="product-icon">
            <div class="icon-bg">🏢</div>
          </div>
          <h3 class="product-title">RiverTI Prop</h3>
          <p class="product-description">
            Software de gestión inmobiliaria con agente de IA. Automatiza la
            administración de propiedades, contratos, pagos y atención a
            clientes con la ayuda de RiverTI Bot, tu asistente inteligente.
          </p>
          <ul class="product-features">
            <li>Gestión de propiedades y contratos</li>
            <li>Agente de IA para consultas y soporte</li>
            <li>Automatización de cobros y pagos</li>
            <li>Panel de control intuitivo</li>
          </ul>
        </div>

        <!-- Aquí puedes agregar más productos en el futuro -->
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section id="servicios" class="services">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">Nuestros Servicios</h2>
        <p class="section-subtitle">
          Soluciones tecnológicas que impulsan el crecimiento de tu empresa
        </p>
      </div>
      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon">
            <div class="icon-bg">⚡</div>
          </div>
          <h3 class="service-title">Automatización</h3>
          <p class="service-description">
            Optimizamos procesos empresariales mediante automatización
            inteligente, reduciendo costos y mejorando la eficiencia
            operacional.
          </p>
          <ul class="service-features">
            <li>RPA (Robotic Process Automation)</li>
            <li>Automatización de workflows</li>
            <li>Integración de sistemas</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">
            <div class="icon-bg">💻</div>
          </div>
          <h3 class="service-title">Software a Medida</h3>
          <p class="service-description">
            Desarrollamos soluciones de software personalizadas que se adaptan
            perfectamente a las necesidades específicas de tu negocio.
          </p>
          <ul class="service-features">
            <li>Aplicaciones web y móviles</li>
            <li>Sistemas de gestión</li>
            <li>APIs y microservicios</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">
            <div class="icon-bg">📊</div>
          </div>
          <h3 class="service-title">Data Science</h3>
          <p class="service-description">
            Transformamos datos en insights valiosos mediante análisis avanzado,
            machine learning y visualizaciones inteligentes.
          </p>
          <ul class="service-features">
            <li>Análisis predictivo</li>
            <li>Machine Learning</li>
            <li>Dashboards interactivos</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">
            <div class="icon-bg">🎯</div>
          </div>
          <h3 class="service-title">Consultoría de TI</h3>
          <p class="service-description">
            Asesoramos en la definición de estrategias tecnológicas que alineen
            la TI con los objetivos de negocio de tu empresa.
          </p>
          <ul class="service-features">
            <li>Arquitectura tecnológica</li>
            <li>Transformación digital</li>
            <li>Auditoría de sistemas</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section id="nosotros" class="about">
    <div class="container">
      <div class="about-content">
        <div class="about-text">
          <h2 class="section-title">Sobre RiverTI</h2>
          <p class="about-description">
            Somos una empresa chilena de tecnología con sede en Providencia,
            dedicada a transformar negocios a través de soluciones tecnológicas
            innovadoras. Nuestro equipo de expertos combina conocimiento técnico
            profundo con una comprensión clara de las necesidades empresariales.
          </p>
          <div class="about-stats">
            <div class="stat">
              <div class="stat-number">5+</div>
              <div class="stat-label">Años de Experiencia</div>
            </div>
            <div class="stat">
              <div class="stat-number">100%</div>
              <div class="stat-label">Satisfacción Cliente</div>
            </div>
          </div>
        </div>
        <div class="about-visual">
          <div class="tech-stack">
            <div class="tech-item">Python</div>
            <div class="tech-item">JavaScript</div>
            <div class="tech-item">React</div>
            <div class="tech-item">Node.js</div>
            <div class="tech-item">AWS</div>
            <div class="tech-item">Docker</div>
            <div class="tech-item">PostgreSQL</div>
            <div class="tech-item">Kubernetes</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contacto" class="contact">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">Comencemos tu Proyecto</h2>
        <p class="section-subtitle">
          ¿Listo para transformar tu negocio? Conversemos sobre tus necesidades
          tecnológicas
        </p>
      </div>
      <div class="contact-content">
        <div class="contact-info">
          <div class="contact-item">
            <div class="contact-icon">📍</div>
            <div>
              <h4>Ubicación</h4>
              <p>Providencia, Santiago<br />Chile</p>
            </div>
          </div>
          <div class="contact-item">
            <div class="contact-icon">📧</div>
            <div>
              <h4>Email</h4>
              <p>contacto@riverti.cl</p>
            </div>
          </div>
          <div class="contact-item">
            <div class="contact-icon">📱</div>
            <div>
              <h4>Teléfono</h4>
              <p>+56 2 2XXX XXXX</p>
            </div>
          </div>
        </div>
        <form class="contact-form" novalidate action="https://api.web3forms.com/submit" method="POST">
          <!-- Web3Forms API Key -->
          <input type="hidden" name="access_key" value="656eb5f7-defa-4560-877c-2ad119c3e14f">
          
          
          <!-- Optional: Subject line for the email -->
          <input type="hidden" name="subject" value="Nueva consulta desde RiverTI Landing">
          
          <!-- Optional: From name -->
          <input type="hidden" name="from_name" value="RiverTI Website">
          
          <!-- Status message -->
          <div class="form-status" id="form-status"></div>
          
          <div class="form-group">
            <label for="name" class="form-label">Nombre completo</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              placeholder="Ingresa tu nombre completo" 
              required 
              autocomplete="name"
            />
            <span class="form-error" id="name-error"></span>
          </div>
          <div class="form-group">
            <label for="email" class="form-label">Email</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              placeholder="tu@email.com" 
              required 
              autocomplete="email"
            />
            <span class="form-error" id="email-error"></span>
          </div>
          <div class="form-group">
            <label for="phone" class="form-label">Teléfono (opcional)</label>
            <input 
              type="tel" 
              id="phone" 
              name="phone" 
              placeholder="+56 9 XXXX XXXX" 
              autocomplete="tel"
            />
          </div>
          <div class="form-group">
            <label for="service" class="form-label">Servicio de interés</label>
            <select id="service" name="service" required>
              <option value="">Selecciona un servicio</option>
              <option value="automatizacion">Automatización</option>
              <option value="software">Software a Medida</option>
              <option value="data-science">Data Science</option>
              <option value="consultoria">Consultoría de TI</option>
              <option value="productos">Productos SAAS</option>
            </select>
            <span class="form-error" id="service-error"></span>
          </div>
          <div class="form-group">
            <label for="message" class="form-label">Mensaje</label>
            <textarea
              id="message"
              name="message"
              placeholder="Cuéntanos sobre tu proyecto y necesidades..."
              rows="4"
              required></textarea>
            <span class="form-error" id="message-error"></span>
          </div>
          
          <!-- Honeypot field for spam protection -->
          <input type="checkbox" name="botcheck" style="display: none;">
          
          <button type="submit" class="btn btn-primary">
            <span class="btn-text">Enviar Consulta</span>
            <span class="btn-loader"></span>
          </button>
        </form>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-brand">
          <span class="logo-text">River<span class="logo-accent">TI</span></span
          >
          <p>Transformando negocios con tecnología inteligente</p>
        </div>
        <div class="footer-links">
          <div class="footer-column">
            <h4>Servicios</h4>
            <a href="#servicios">Automatización</a>
            <a href="#servicios">Software a Medida</a>
            <a href="#servicios">Data Science</a>
            <a href="#servicios">Consultoría de TI</a>
          </div>
          <div class="footer-column">
            <h4>Productos</h4>
            <a href="#productos">RiverTI Prop</a>
            <a href="#productos">RiverTI Car</a>
          </div>
          <div class="footer-column">
            <h4>Empresa</h4>
            <a href="#nosotros">Sobre Nosotros</a>
            <a href="#contacto">Contacto</a>
          </div>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2025 RiverTI. Todos los derechos reservados.</p>
      </div>
    </div>
  </footer>
</Layout>

<style>
  /* Variables */
  :root {
    --primary: #0066cc;
    --primary-dark: #004499;
    --secondary: #00cc66;
    --accent: #ff6b35;
    --dark: #0a0a0a;
    --dark-light: #1a1a1a;
    --gray: #666666;
    --light: #ffffff;
    --spacing: 8px;
    --border-radius: 12px;
    --transition: all 0.3s ease;
    --shadow: 0 10px 40px rgba(0, 102, 204, 0.15);
    --shadow-hover: 0 20px 60px rgba(0, 102, 204, 0.25);
  }

  /* Navigation */
  .nav {
    position: fixed;
    top: 0;
    width: 100%;
    background: rgba(10, 10, 10, 0.95);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    z-index: 1000;
    transition: var(--transition);
  }

  .nav-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: calc(var(--spacing) * 2) calc(var(--spacing) * 3);
  }

  .logo-text {
    font-size: 24px;
    font-weight: 700;
    color: var(--light);
  }

  .logo-accent {
    color: var(--primary);
  }

  .nav-menu {
    display: flex;
    gap: calc(var(--spacing) * 4);
  }

  .nav-link {
    color: var(--light);
    text-decoration: none;
    font-weight: 500;
    transition: var(--transition);
    position: relative;
  }

  .nav-link::after {
    content: "";
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--primary);
    transition: var(--transition);
  }

  .nav-link:hover::after {
    width: 100%;
  }

  .nav-toggle {
    display: none;
    flex-direction: column;
    cursor: pointer;
  }

  .nav-toggle span {
    width: 25px;
    height: 2px;
    background: var(--light);
    margin: 3px 0;
    transition: var(--transition);
  }

  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      var(--dark) 0%,
      #1a1a2e 50%,
      var(--dark) 100%
    );
    z-index: -2;
  }

  .grid-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: linear-gradient(
        rgba(0, 102, 204, 0.1) 1px,
        transparent 1px
      ),
      linear-gradient(90deg, rgba(0, 102, 204, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    animation: float 20s ease-in-out infinite;
    z-index: -1;
  }

  .hero-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 calc(var(--spacing) * 3);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(var(--spacing) * 8);
    align-items: center;
  }

  .hero-content {
    animation: slideInLeft 1s ease-out;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: calc(var(--spacing) * 3);
    color: var(--light);
  }

  .gradient-text {
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--gray);
    margin-bottom: calc(var(--spacing) * 5);
    line-height: 1.6;
  }

  .hero-cta {
    display: flex;
    gap: calc(var(--spacing) * 3);
    flex-wrap: wrap;
  }

  /* Buttons */
  .btn {
    display: inline-block;
    padding: calc(var(--spacing) * 2) calc(var(--spacing) * 4);
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: var(--transition);
    border: 2px solid transparent;
    cursor: pointer;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: var(--light);
    box-shadow: var(--shadow);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-hover);
  }

  .btn-secondary {
    background: transparent;
    color: var(--light);
    border-color: var(--primary);
  }

  .btn-secondary:hover {
    background: var(--primary);
    transform: translateY(-2px);
  }

  /* Hero Visual */
  .hero-visual {
    position: relative;
    height: 500px;
    animation: slideInRight 1s ease-out;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .gradient-orb {
    position: absolute;
    width: 300px;
    height: 300px;
    border-radius: 50%;
    background: radial-gradient(
      circle at 30% 30%,
      rgba(0, 102, 204, 0.8) 0%,
      rgba(0, 204, 102, 0.6) 35%,
      rgba(255, 107, 53, 0.4) 70%,
      transparent 100%
    );
    filter: blur(40px);
    animation: pulse 4s ease-in-out infinite;
  }

  .gradient-mesh {
    position: absolute;
    width: 400px;
    height: 400px;
    background: conic-gradient(
      from 0deg at 50% 50%,
      rgba(0, 102, 204, 0.3) 0deg,
      rgba(0, 204, 102, 0.2) 120deg,
      rgba(255, 107, 53, 0.3) 240deg,
      rgba(0, 102, 204, 0.3) 360deg
    );
    border-radius: 50%;
    filter: blur(60px);
    animation: rotate 20s linear infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.8;
    }
    50% {
      transform: scale(1.1);
      opacity: 0.6;
    }
  }

  @keyframes rotate {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  /* Services Section */
  .services {
    padding: calc(var(--spacing) * 12) 0;
    background: var(--dark-light);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 calc(var(--spacing) * 3);
  }

  .section-header {
    text-align: center;
    margin-bottom: calc(var(--spacing) * 8);
  }

  .section-title {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: calc(var(--spacing) * 2);
    color: var(--light);
  }

  .section-subtitle {
    font-size: 1.25rem;
    color: var(--gray);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: calc(var(--spacing) * 4);
  }

  .service-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius);
    padding: calc(var(--spacing) * 4);
    transition: var(--transition);
    position: relative;
    overflow: hidden;
  }

  .service-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--primary), var(--secondary));
    transform: scaleX(0);
    transition: var(--transition);
  }

  .service-card:hover::before {
    transform: scaleX(1);
  }

  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-hover);
  }

  .service-icon {
    margin-bottom: calc(var(--spacing) * 3);
  }

  .icon-bg {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
  }

  .service-title {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: calc(var(--spacing) * 2);
    color: var(--light);
  }

  .service-description {
    color: var(--gray);
    line-height: 1.6;
    margin-bottom: calc(var(--spacing) * 3);
  }

  .service-features {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .service-features li {
    color: var(--gray);
    margin-bottom: calc(var(--spacing));
    padding-left: calc(var(--spacing) * 3);
    position: relative;
  }

  .service-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--secondary);
    font-weight: bold;
  }

  /* Products Section */
  .products {
    padding: calc(var(--spacing) * 12) 0;
    background: var(--dark);
  }

  .products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: calc(var(--spacing) * 4);
  }

  .product-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius);
    padding: calc(var(--spacing) * 4);
    transition: var(--transition);
    position: relative;
    overflow: hidden;
  }

  .product-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--primary), var(--secondary));
    transform: scaleX(0);
    transition: var(--transition);
  }

  .product-card:hover::before {
    transform: scaleX(1);
  }

  .product-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-hover);
  }

  .product-icon {
    margin-bottom: calc(var(--spacing) * 3);
  }

  .product-title {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: calc(var(--spacing) * 2);
    color: var(--light);
  }

  .product-description {
    color: var(--gray);
    line-height: 1.6;
    margin-bottom: calc(var(--spacing) * 3);
  }

  .product-features {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .product-features li {
    color: var(--gray);
    margin-bottom: calc(var(--spacing));
    padding-left: calc(var(--spacing) * 3);
    position: relative;
  }

  .product-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--secondary);
    font-weight: bold;
  }

  /* About Section */
  .about {
    padding: calc(var(--spacing) * 12) 0;
    background: var(--dark-light);
  }

  .about-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(var(--spacing) * 8);
    align-items: center;
  }

  .about-description {
    font-size: 1.125rem;
    color: var(--gray);
    line-height: 1.6;
    margin-bottom: calc(var(--spacing) * 4);
  }

  .about-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: calc(var(--spacing) * 3);
  }

  .stat {
    text-align: center;
  }

  .stat-number {
    font-size: 2rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: calc(var(--spacing));
  }

  .stat-label {
    color: var(--gray);
    font-size: 0.9rem;
  }

  .tech-stack {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: calc(var(--spacing) * 2);
  }

  .tech-item {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius);
    padding: calc(var(--spacing) * 2);
    text-align: center;
    color: var(--light);
    font-weight: 500;
    transition: var(--transition);
  }

  .tech-item:hover {
    background: rgba(0, 102, 204, 0.1);
    border-color: var(--primary);
    transform: translateY(-2px);
  }

  /* Contact Section */
  .contact {
    padding: calc(var(--spacing) * 12) 0;
    background: var(--dark-light);
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(var(--spacing) * 8);
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing) * 4);
  }

  .contact-item {
    display: flex;
    align-items: flex-start;
    gap: calc(var(--spacing) * 2);
  }

  .contact-icon {
    font-size: 1.5rem;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .contact-item h4 {
    color: var(--light);
    margin: 0 0 calc(var(--spacing)) 0;
    font-weight: 600;
  }

  .contact-item p {
    color: var(--gray);
    margin: 0;
    line-height: 1.5;
  }

  /* Contact Form Styles */
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing) * 3);
    max-width: 100%;
  }

  .form-group {
    position: relative;
    width: 100%;
  }

  .form-label {
    display: block;
    margin-bottom: calc(var(--spacing));
    color: var(--light);
    font-weight: 500;
    font-size: 0.9rem;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: calc(var(--spacing) * 2);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: var(--border-radius);
    background: rgba(255, 255, 255, 0.05);
    color: var(--light);
    font-family: inherit;
    font-size: 1rem;
    line-height: 1.5;
    transition: var(--transition);
    box-sizing: border-box;
    
    /* Cross-browser compatibility */
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
    
    /* Backdrop filter with fallbacks */
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }

  /* Select arrow styling for cross-browser compatibility */
  .form-group select {
    background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23666666' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6,9 12,15 18,9'%3e%3c/polyline%3e%3c/svg%3e");
    background-repeat: no-repeat;
    background-position: right calc(var(--spacing) * 2) center;
    background-size: 20px;
    padding-right: calc(var(--spacing) * 5);
  }

  /* Textarea specific styles */
  .form-group textarea {
    resize: vertical;
    min-height: 100px;
    font-family: inherit;
  }

  /* Focus states */
  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 3px rgba(0, 102, 204, 0.1);
    background: rgba(255, 255, 255, 0.08);
  }

  /* Placeholder styles */
  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: var(--gray);
    opacity: 1; /* Firefox fix */
  }

  .form-group input::-webkit-input-placeholder,
  .form-group textarea::-webkit-input-placeholder {
    color: var(--gray);
  }

  .form-group input::-moz-placeholder,
  .form-group textarea::-moz-placeholder {
    color: var(--gray);
    opacity: 1;
  }

  .form-group input:-ms-input-placeholder,
  .form-group textarea:-ms-input-placeholder {
    color: var(--gray);
  }

  /* Error states */
  .form-error {
    display: block;
    color: var(--accent);
    font-size: 0.85rem;
    margin-top: calc(var(--spacing) / 2);
    min-height: 1.2em;
    opacity: 0;
    transition: opacity 0.3s ease;
    visibility: hidden;
  }

  .form-group.error .form-error {
    opacity: 1;
    visibility: visible;
  }

  .form-group.error input,
  .form-group.error select,
  .form-group.error textarea {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(255, 107, 53, 0.1);
  }

  /* Valid states */
  .form-group.valid input,
  .form-group.valid select,
  .form-group.valid textarea {
    border-color: var(--secondary);
  }

  /* Touched state - only show errors after user interaction */
  .form-group:not(.touched) .form-error {
    display: none;
  }

  /* Button loading state */
  .btn {
    position: relative;
    overflow: hidden;
  }

  .btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  .btn .btn-loader {
    display: none;
    width: 20px;
    height: 20px;
    border: 2px solid transparent;
    border-top: 2px solid currentColor;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin-left: calc(var(--spacing));
  }

  .btn.loading .btn-text {
    opacity: 0.7;
  }

  .btn.loading .btn-loader {
    display: inline-block;
  }

  /* Success and error button states */
  .btn.success {
    background: var(--secondary) !important;
    border-color: var(--secondary) !important;
  }

  .btn.error {
    background: var(--accent) !important;
    border-color: var(--accent) !important;
  }

  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }

  /* Form status messages */
  .form-status {
    padding: calc(var(--spacing) * 2);
    border-radius: var(--border-radius);
    margin-bottom: calc(var(--spacing) * 3);
    font-weight: 500;
    display: none;
  }

  .form-status.success {
    background: rgba(0, 204, 102, 0.1);
    border: 1px solid rgba(0, 204, 102, 0.3);
    color: var(--secondary);
  }

  .form-status.error {
    background: rgba(255, 107, 53, 0.1);
    border: 1px solid rgba(255, 107, 53, 0.3);
    color: var(--accent);
  }

  /* Responsive improvements */
  @media (max-width: 768px) {
    .contact-form {
      gap: calc(var(--spacing) * 2);
    }
    
    .form-group input,
    .form-group select,
    .form-group textarea {
      font-size: 16px; /* Prevents zoom on iOS */
    }
  }

  /* Footer */
  .footer {
    background: var(--dark);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding: calc(var(--spacing) * 8) 0 calc(var(--spacing) * 3) 0;
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(var(--spacing) * 8);
    margin-bottom: calc(var(--spacing) * 4);
  }

  .footer-brand p {
    color: var(--gray);
    margin-top: calc(var(--spacing) * 2);
  }

  .footer-links {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: calc(var(--spacing) * 4);
  }

  .footer-column h4 {
    color: var(--light);
    margin-bottom: calc(var(--spacing) * 2);
    font-weight: 600;
  }

  .footer-column a {
    display: block;
    color: var(--gray);
    text-decoration: none;
    margin-bottom: calc(var(--spacing));
    transition: var(--transition);
  }

  .footer-column a:hover {
    color: var(--primary);
  }

  .footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: calc(var(--spacing) * 3);
    text-align: center;
    color: var(--gray);
  }

  /* Animations */
  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .hero-container,
    .about-content,
    .contact-content {
      grid-template-columns: 1fr;
      gap: calc(var(--spacing) * 4);
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .tech-stack {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .nav-menu {
      display: none;
    }

    .nav-toggle {
      display: flex;
    }

    .hero-title {
      font-size: 2rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .about-stats {
      grid-template-columns: 1fr;
    }

    .footer-content {
      grid-template-columns: 1fr;
    }

    .footer-links {
      grid-template-columns: 1fr;
    }

    .hero-cta {
      flex-direction: column;
    }

    .btn {
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .hero-visual {
      height: 300px;
    }

    .gradient-orb {
      width: 200px;
      height: 200px;
    }

    .gradient-mesh {
      width: 250px;
      height: 250px;
    }
  }
</style>

<script is:inline>
  // Cross-browser compatibility
  (function() {
    'use strict';

    // Wait for DOM to be ready
    function ready(fn) {
      if (document.readyState !== 'loading') {
        fn();
      } else {
        document.addEventListener('DOMContentLoaded', fn);
      }
    }

    ready(function() {
      // Smooth scrolling for navigation links
      var links = document.querySelectorAll('a[href^="#"]');
      for (var i = 0; i < links.length; i++) {
        links[i].addEventListener("click", function(e) {
          e.preventDefault();
          var href = this.getAttribute("href");
          if (href) {
            var target = document.querySelector(href);
            if (target) {
              target.scrollIntoView({
                behavior: "smooth",
                block: "start",
              });
            }
          }
        });
      }

      // Form validation and submission
      var form = document.querySelector(".contact-form");
      if (form) {
        var inputs = form.querySelectorAll('input, select, textarea');
        var submitBtn = form.querySelector('button[type="submit"]');
        var statusDiv = form.querySelector('#form-status');
        
        // Email validation regex
        var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        
        // Phone validation regex (Chilean format)
        var phoneRegex = /^(\+56|56)?[2-9]\d{8}$/;

        // Function to show status message
        function showStatus(message, type) {
          if (statusDiv) {
            statusDiv.textContent = message;
            statusDiv.className = 'form-status ' + type;
            statusDiv.style.display = 'block';
            
            // Auto-hide after 5 seconds for success messages
            if (type === 'success') {
              setTimeout(function() {
                statusDiv.style.display = 'none';
              }, 5000);
            }
          }
        }

        function hideStatus() {
          if (statusDiv) {
            statusDiv.style.display = 'none';
          }
        }

        // Validation functions
        function validateField(field, showErrors) {
          if (!field) return false;
          
          if (showErrors === undefined) showErrors = true;
          
          var value = field.value ? field.value.trim() : '';
          var fieldName = field.name || '';
          var formGroup = field.closest('.form-group');
          var isRequired = field.hasAttribute('required');
          
          if (!formGroup) return false;
          
          var errorElement = formGroup.querySelector('.form-error');
          
          var isValid = true;
          var errorMessage = '';

          // Clear previous states
          formGroup.classList.remove('error', 'valid');
          
          // Only validate if field is required OR if it has content
          if (isRequired || value) {
            if (isRequired && !value) {
              isValid = false;
              errorMessage = 'Este campo es obligatorio';
            } else if (fieldName === 'email' && value && !emailRegex.test(value)) {
              isValid = false;
              errorMessage = 'Por favor ingresa un email válido';
            } else if (fieldName === 'phone' && value && !phoneRegex.test(value.replace(/\s/g, ''))) {
              isValid = false;
              errorMessage = 'Por favor ingresa un teléfono válido (+56 9 XXXX XXXX)';
            } else if (fieldName === 'name' && value && value.length < 2) {
              isValid = false;
              errorMessage = 'El nombre debe tener al menos 2 caracteres';
            } else if (fieldName === 'message' && value && value.length < 10) {
              isValid = false;
              errorMessage = 'El mensaje debe tener al menos 10 caracteres';
            } else if (fieldName === 'service' && isRequired && !value) {
              isValid = false;
              errorMessage = 'Por favor selecciona un servicio';
            }
          }

          if (errorElement) {
            errorElement.textContent = errorMessage;
          }
          
          // Only show validation states if the field has been touched and we should show errors
          if (formGroup.classList.contains('touched') && showErrors) {
            if (isValid && value) {
              formGroup.classList.add('valid');
            } else if (!isValid) {
              formGroup.classList.add('error');
            }
          }

          // For required fields, return false if empty, for optional fields return true if empty
          if (!isRequired && !value) {
            return true;
          }

          return isValid;
        }

        function validateForm(showErrors) {
          if (showErrors === undefined) showErrors = false;
          
          var isFormValid = true;
          
          // Check required fields specifically
          var nameField = form.querySelector('#name');
          var emailField = form.querySelector('#email');
          var serviceField = form.querySelector('#service');
          var messageField = form.querySelector('#message');
          
          // Validate each required field
          if (!nameField || !nameField.value.trim()) {
            isFormValid = false;
          } else if (!validateField(nameField, showErrors)) {
            isFormValid = false;
          }
          
          if (!emailField || !emailField.value.trim()) {
            isFormValid = false;
          } else if (!validateField(emailField, showErrors)) {
            isFormValid = false;
          }
          
          if (!serviceField || !serviceField.value) {
            isFormValid = false;
          }
          
          if (!messageField || !messageField.value.trim()) {
            isFormValid = false;
          } else if (!validateField(messageField, showErrors)) {
            isFormValid = false;
          }
          
          // Validate optional phone field if it has content
          var phoneField = form.querySelector('#phone');
          if (phoneField && phoneField.value.trim()) {
            validateField(phoneField, showErrors);
          }
          
          // Enable/disable submit button
          if (submitBtn) {
            submitBtn.disabled = !isFormValid;
          }
          
          return isFormValid;
        }

        // Add event listeners to inputs
        for (var i = 0; i < inputs.length; i++) {
          var input = inputs[i];
          
          // Mark field as touched when user starts interacting
          input.addEventListener('focus', function() {
            var formGroup = this.closest('.form-group');
            if (formGroup) {
              formGroup.classList.add('touched');
            }
          });

          // Real-time validation on blur (when user leaves the field)
          input.addEventListener('blur', function() {
            var formGroup = this.closest('.form-group');
            if (formGroup) {
              formGroup.classList.add('touched');
            }
            validateField(this, true);
            validateForm(false);
          });

          // Validate while typing (but only show errors if already touched)
          input.addEventListener('input', function() {
            var field = this;
            var formGroup = field.closest('.form-group');
            
            // Always update button state when user types
            setTimeout(function() {
              if (formGroup && formGroup.classList.contains('touched')) {
                validateField(field, true);
              }
              validateForm(false);
            }, 100);
          });
        }

        // Form submission
        form.addEventListener("submit", function(e) {
          e.preventDefault();
          
          // Hide any previous status messages
          hideStatus();
          
          // Mark all fields as touched and validate with errors
          for (var i = 0; i < inputs.length; i++) {
            var input = inputs[i];
            var formGroup = input.closest('.form-group');
            if (formGroup) {
              formGroup.classList.add('touched');
            }
          }
          
          if (!validateForm(true)) {
            // Focus on first invalid field
            var firstInvalidField = form.querySelector('.form-group.error input, .form-group.error select, .form-group.error textarea');
            if (firstInvalidField && firstInvalidField.focus) {
              firstInvalidField.focus();
            }
            showStatus('Por favor, completa todos los campos requeridos correctamente.', 'error');
            return;
          }

          var btnText = submitBtn ? submitBtn.querySelector('.btn-text') : null;

          // Show loading state
          if (submitBtn) {
            submitBtn.classList.add('loading');
            submitBtn.classList.remove('success', 'error');
            submitBtn.disabled = true;
          }
          if (btnText) {
            btnText.textContent = "Enviando...";
          }

          // Create FormData object
          var formData = new FormData(form);

          // Submit to Web3Forms
          fetch('https://api.web3forms.com/submit', {
            method: 'POST',
            headers: { 'Accept': 'application/json' },
            body: formData
          })
          .then(function(response) {
            return response.json();
          })
          .then(function(data) {
            if (data.success) {
              // Success state
              if (submitBtn) {
                submitBtn.classList.remove('loading');
                submitBtn.classList.add('success');
              }
              if (btnText) {
                btnText.textContent = "¡Enviado!";
              }
              
              // Show success message
              showStatus('¡Gracias por tu consulta! Tu mensaje ha sido enviado exitosamente. Nos pondremos en contacto contigo pronto.', 'success');

              // Confirmación simple sin redirección
              alert("Gracias por tu consulta. Te contactaremos pronto.");
              
              // Reset form
              form.reset();
              
              for (var i = 0; i < inputs.length; i++) {
                var input = inputs[i];
                var formGroup = input.closest('.form-group');
                if (formGroup) {
                  formGroup.classList.remove('error', 'valid', 'touched');
                  var errorElement = formGroup.querySelector('.form-error');
                  if (errorElement) {
                    errorElement.textContent = '';
                  }
                }
              }
              
              // Reset button
              setTimeout(function() {
                if (btnText) {
                  btnText.textContent = "Enviar Consulta";
                }
                if (submitBtn) {
                  submitBtn.classList.remove('success');
                  submitBtn.disabled = true; // Keep disabled until form is valid again
                }
              }, 3000);
              
            } else {
              // Error from Web3Forms
              throw new Error(data.message || 'Error al enviar el formulario');
            }
          })
          .catch(function(error) {
            console.error('Error:', error);
            
            // Error state
            if (submitBtn) {
              submitBtn.classList.remove('loading');
              submitBtn.classList.add('error');
            }
            if (btnText) {
              btnText.textContent = "Error al enviar";
            }
            
            // Show error message
            showStatus('Hubo un problema al enviar tu consulta. Por favor, inténtalo de nuevo o contáctanos directamente a contacto@riverti.cl', 'error');
            
            // Reset button after error
            setTimeout(function() {
              if (btnText) {
                btnText.textContent = "Enviar Consulta";
              }
              if (submitBtn) {
                submitBtn.classList.remove('error');
                submitBtn.disabled = false;
              }
            }, 3000);
          });
        });

        // Initial setup - enable button state checking
        if (submitBtn) {
          submitBtn.disabled = true; // Start disabled
        }
        
        // Initial validation (without showing errors)
        validateForm(false);
      }

      // Navigation scroll effect
      function handleNavScroll() {
        var nav = document.querySelector(".nav");
        if (nav) {
          var scrolled = window.pageYOffset || document.documentElement.scrollTop;
          if (scrolled > 50) {
            nav.style.background = "rgba(10, 10, 10, 0.98)";
          } else {
            nav.style.background = "rgba(10, 10, 10, 0.95)";
          }
        }
      }

      // Add scroll listener with throttling for better performance
      var scrollTimeout = null;
      window.addEventListener("scroll", function() {
        if (!scrollTimeout) {
          scrollTimeout = setTimeout(function() {
            handleNavScroll();
            scrollTimeout = null;
          }, 10);
        }
      });

      // Mobile navigation toggle (if needed in the future)
      var navToggle = document.querySelector('.nav-toggle');
      var navMenu = document.querySelector('.nav-menu');
      
      if (navToggle && navMenu) {
        navToggle.addEventListener('click', function() {
          navMenu.classList.toggle('active');
        });
      }
    });

  })();
</script>
