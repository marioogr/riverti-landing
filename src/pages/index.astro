---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <!-- Navigation -->
  <nav class="nav">
    <div class="nav-container">
      <div class="nav-logo">
        <span class="logo-text">River<span class="logo-accent">TI</span></span>
      </div>
      <div class="nav-menu">
        <a href="#inicio" class="nav-link">Inicio</a>
        <a href="#servicios" class="nav-link">Servicios</a>
        <a href="#productos" class="nav-link">Productos</a>
        <a href="#nosotros" class="nav-link">Nosotros</a>
        <a href="#contacto" class="nav-link">Contacto</a>
      </div>
      <div class="nav-toggle">
        <span></span>
        <span></span>
        <span></span>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section id="inicio" class="hero">
    <div class="hero-background">
      <div class="grid-overlay"></div>
    </div>
    <div class="hero-container">
      <div class="hero-content">
        <h1 class="hero-title">
          Transformamos tu negocio con
          <span class="gradient-text">tecnología inteligente</span>
        </h1>
        <p class="hero-subtitle">
          Automatización, software a medida, data science y consultoría de TI.
        </p>
        <div class="hero-cta">
          <a href="#contacto" class="btn btn-primary">Comenzar Proyecto</a>
          <a href="#servicios" class="btn btn-secondary">Ver Servicios</a>
        </div>
      </div>
      <div class="hero-visual">
        <div class="gradient-orb"></div>
        <div class="gradient-mesh"></div>
      </div>
    </div>
  </section>

  <!-- Productos / SAAS Section -->
  <section id="productos" class="products">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">Nuestros Productos</h2>
        <p class="section-subtitle">
          Soluciones SAAS innovadoras para potenciar tu negocio
        </p>
      </div>
      <div class="products-grid">
        <div class="product-card">
          <div class="product-icon">
            <div class="icon-bg">🏢</div>
          </div>
          <h3 class="product-title">RiverTI Prop</h3>
          <p class="product-description">
            Software de gestión inmobiliaria con agente de IA. Automatiza la administración de propiedades, contratos, pagos y atención a clientes con la ayuda de RiverTI Prop, tu asistente inteligente.
          </p>
          <ul class="product-features">
            <li>Gestión de propiedades y contratos</li>
            <li>Agente de IA para consultas y soporte</li>
            <li>Automatización de cobros y pagos</li>
            <li>Panel de control intuitivo</li>
          </ul>
        </div>
        <!-- Aquí puedes agregar más productos en el futuro -->
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section id="servicios" class="services">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">Nuestros Servicios</h2>
        <p class="section-subtitle">
          Soluciones tecnológicas que impulsan el crecimiento de tu empresa
        </p>
      </div>
      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon">
            <div class="icon-bg">⚡</div>
          </div>
          <h3 class="service-title">Automatización</h3>
          <p class="service-description">
            Optimizamos procesos empresariales mediante automatización
            inteligente, reduciendo costos y mejorando la eficiencia
            operacional.
          </p>
          <ul class="service-features">
            <li>RPA (Robotic Process Automation)</li>
            <li>Automatización de workflows</li>
            <li>Integración de sistemas</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">
            <div class="icon-bg">💻</div>
          </div>
          <h3 class="service-title">Software a Medida</h3>
          <p class="service-description">
            Desarrollamos soluciones de software personalizadas que se adaptan
            perfectamente a las necesidades específicas de tu negocio.
          </p>
          <ul class="service-features">
            <li>Aplicaciones web y móviles</li>
            <li>Sistemas de gestión</li>
            <li>APIs y microservicios</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">
            <div class="icon-bg">📊</div>
          </div>
          <h3 class="service-title">Data Science</h3>
          <p class="service-description">
            Transformamos datos en insights valiosos mediante análisis avanzado,
            machine learning y visualizaciones inteligentes.
          </p>
          <ul class="service-features">
            <li>Análisis predictivo</li>
            <li>Machine Learning</li>
            <li>Dashboards interactivos</li>
          </ul>
        </div>

        <div class="service-card">
          <div class="service-icon">
            <div class="icon-bg">🎯</div>
          </div>
          <h3 class="service-title">Consultoría de TI</h3>
          <p class="service-description">
            Asesoramos en la definición de estrategias tecnológicas que alineen
            la TI con los objetivos de negocio de tu empresa.
          </p>
          <ul class="service-features">
            <li>Arquitectura tecnológica</li>
            <li>Transformación digital</li>
            <li>Auditoría de sistemas</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section id="nosotros" class="about">
    <div class="container">
      <div class="about-content">
        <div class="about-text">
          <h2 class="section-title">Sobre RiverTI</h2>
          <p class="about-description">
            Somos una empresa chilena de tecnología con sede en Providencia,
            dedicada a transformar negocios a través de soluciones tecnológicas
            innovadoras. Nuestro equipo de expertos combina conocimiento técnico
            profundo con una comprensión clara de las necesidades empresariales.
          </p>
          <div class="about-stats">
            <div class="stat">
              <div class="stat-number">5+</div>
              <div class="stat-label">Años de Experiencia</div>
            </div>
            <div class="stat">
              <div class="stat-number">100%</div>
              <div class="stat-label">Satisfacción Cliente</div>
            </div>
          </div>
        </div>
        <div class="about-visual">
          <div class="tech-stack">
            <div class="tech-item">Python</div>
            <div class="tech-item">JavaScript</div>
            <div class="tech-item">React</div>
            <div class="tech-item">Node.js</div>
            <div class="tech-item">AWS</div>
            <div class="tech-item">Docker</div>
            <div class="tech-item">PostgreSQL</div>
            <div class="tech-item">Kubernetes</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contacto" class="contact">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">Comencemos tu Proyecto</h2>
        <p class="section-subtitle">
          ¿Listo para transformar tu negocio? Conversemos sobre tus necesidades
          tecnológicas
        </p>
      </div>
      <div class="contact-content">
        <div class="contact-info">
          <div class="contact-item">
            <div class="contact-icon">📍</div>
            <div>
              <h4>Ubicación</h4>
              <p>Providencia, Santiago<br />Chile</p>
            </div>
          </div>
          <div class="contact-item">
            <div class="contact-icon">📧</div>
            <div>
              <h4>Email</h4>
              <p>info@rivetti.cl</p>
            </div>
          </div>
          <div class="contact-item">
            <div class="contact-icon">📱</div>
            <div>
              <h4>Teléfono</h4>
              <p>+56 2 2XXX XXXX</p>
            </div>
          </div>
        </div>
        <form class="contact-form">
          <div class="form-group">
            <input type="text" placeholder="Nombre completo" required />
          </div>
          <div class="form-group">
            <input type="email" placeholder="Email" required />
          </div>
          <div class="form-group">
            <input type="tel" placeholder="Teléfono" />
          </div>
          <div class="form-group">
            <select required>
              <option value="">Selecciona un servicio</option>
              <option value="automatizacion">Automatización</option>
              <option value="software">Software a Medida</option>
              <option value="data-science">Data Science</option>
              <option value="consultoria">Consultoría de TI</option>
            </select>
          </div>
          <div class="form-group">
            <textarea
              placeholder="Cuéntanos sobre tu proyecto"
              rows="4"
              required></textarea>
          </div>
          <button type="submit" class="btn btn-primary">Enviar Consulta</button>
        </form>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-brand">
          <span class="logo-text">River<span class="logo-accent">TI</span></span
          >
          <p>Transformando negocios con tecnología inteligente</p>
        </div>
        <div class="footer-links">
          <div class="footer-column">
            <h4>Servicios</h4>
            <a href="#servicios">Automatización</a>
            <a href="#servicios">Software a Medida</a>
            <a href="#servicios">Data Science</a>
            <a href="#servicios">Consultoría de TI</a>
          </div>
          <div class="footer-column">
            <h4>Productos</h4>
            <a href="#productos">RiverTI Prop</a>
          </div>
          <div class="footer-column">
            <h4>Empresa</h4>
            <a href="#nosotros">Sobre Nosotros</a>
            <a href="#contacto">Contacto</a>
          </div>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2025 RiverTI. Todos los derechos reservados.</p>
      </div>
    </div>
  </footer>
</Layout>

<style>
  /* Variables */
  :root {
    --primary: #0066cc;
    --primary-dark: #004499;
    --secondary: #00cc66;
    --accent: #ff6b35;
    --dark: #0a0a0a;
    --dark-light: #1a1a1a;
    --gray: #666666;
    --light: #ffffff;
    --spacing: 8px;
    --border-radius: 12px;
    --transition: all 0.3s ease;
    --shadow: 0 10px 40px rgba(0, 102, 204, 0.15);
    --shadow-hover: 0 20px 60px rgba(0, 102, 204, 0.25);
  }

  /* Navigation */
  .nav {
    position: fixed;
    top: 0;
    width: 100%;
    background: rgba(10, 10, 10, 0.95);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    z-index: 1000;
    transition: var(--transition);
  }

  .nav-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: calc(var(--spacing) * 2) calc(var(--spacing) * 3);
  }

  .logo-text {
    font-size: 24px;
    font-weight: 700;
    color: var(--light);
  }

  .logo-accent {
    color: var(--primary);
  }

  .nav-menu {
    display: flex;
    gap: calc(var(--spacing) * 4);
  }

  .nav-link {
    color: var(--light);
    text-decoration: none;
    font-weight: 500;
    transition: var(--transition);
    position: relative;
  }

  .nav-link::after {
    content: "";
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--primary);
    transition: var(--transition);
  }

  .nav-link:hover::after {
    width: 100%;
  }

  .nav-toggle {
    display: none;
    flex-direction: column;
    cursor: pointer;
  }

  .nav-toggle span {
    width: 25px;
    height: 2px;
    background: var(--light);
    margin: 3px 0;
    transition: var(--transition);
  }

  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      var(--dark) 0%,
      #1a1a2e 50%,
      var(--dark) 100%
    );
    z-index: -2;
  }

  .grid-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: linear-gradient(
        rgba(0, 102, 204, 0.1) 1px,
        transparent 1px
      ),
      linear-gradient(90deg, rgba(0, 102, 204, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    animation: float 20s ease-in-out infinite;
    z-index: -1;
  }

  .hero-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 calc(var(--spacing) * 3);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(var(--spacing) * 8);
    align-items: center;
  }

  .hero-content {
    animation: slideInLeft 1s ease-out;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: calc(var(--spacing) * 3);
    color: var(--light);
  }

  .gradient-text {
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--gray);
    margin-bottom: calc(var(--spacing) * 5);
    line-height: 1.6;
  }

  .hero-cta {
    display: flex;
    gap: calc(var(--spacing) * 3);
    flex-wrap: wrap;
  }

  /* Buttons */
  .btn {
    display: inline-block;
    padding: calc(var(--spacing) * 2) calc(var(--spacing) * 4);
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: var(--transition);
    border: 2px solid transparent;
    cursor: pointer;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: var(--light);
    box-shadow: var(--shadow);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-hover);
  }

  .btn-secondary {
    background: transparent;
    color: var(--light);
    border-color: var(--primary);
  }

  .btn-secondary:hover {
    background: var(--primary);
    transform: translateY(-2px);
  }

  /* Hero Visual */
  .hero-visual {
    position: relative;
    height: 500px;
    animation: slideInRight 1s ease-out;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .gradient-orb {
    position: absolute;
    width: 300px;
    height: 300px;
    border-radius: 50%;
    background: radial-gradient(
      circle at 30% 30%,
      rgba(0, 102, 204, 0.8) 0%,
      rgba(0, 204, 102, 0.6) 35%,
      rgba(255, 107, 53, 0.4) 70%,
      transparent 100%
    );
    filter: blur(40px);
    animation: pulse 4s ease-in-out infinite;
  }

  .gradient-mesh {
    position: absolute;
    width: 400px;
    height: 400px;
    background: conic-gradient(
      from 0deg at 50% 50%,
      rgba(0, 102, 204, 0.3) 0deg,
      rgba(0, 204, 102, 0.2) 120deg,
      rgba(255, 107, 53, 0.3) 240deg,
      rgba(0, 102, 204, 0.3) 360deg
    );
    border-radius: 50%;
    filter: blur(60px);
    animation: rotate 20s linear infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.8;
    }
    50% {
      transform: scale(1.1);
      opacity: 0.6;
    }
  }

  @keyframes rotate {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  /* Services Section */
  .services {
    padding: calc(var(--spacing) * 12) 0;
    background: var(--dark-light);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 calc(var(--spacing) * 3);
  }

  .section-header {
    text-align: center;
    margin-bottom: calc(var(--spacing) * 8);
  }

  .section-title {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: calc(var(--spacing) * 2);
    color: var(--light);
  }

  .section-subtitle {
    font-size: 1.25rem;
    color: var(--gray);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: calc(var(--spacing) * 4);
  }

  .service-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius);
    padding: calc(var(--spacing) * 4);
    transition: var(--transition);
    position: relative;
    overflow: hidden;
  }

  .service-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--primary), var(--secondary));
    transform: scaleX(0);
    transition: var(--transition);
  }

  .service-card:hover::before {
    transform: scaleX(1);
  }

  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-hover);
  }

  .service-icon {
    margin-bottom: calc(var(--spacing) * 3);
  }

  .icon-bg {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
  }

  .service-title {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: calc(var(--spacing) * 2);
    color: var(--light);
  }

  .service-description {
    color: var(--gray);
    line-height: 1.6;
    margin-bottom: calc(var(--spacing) * 3);
  }

  .service-features {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .service-features li {
    color: var(--gray);
    margin-bottom: calc(var(--spacing));
    padding-left: calc(var(--spacing) * 3);
    position: relative;
  }

  .service-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--secondary);
    font-weight: bold;
  }

  /* Products Section */
  .products {
    padding: calc(var(--spacing) * 12) 0;
    background: var(--dark);
  }

  .products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: calc(var(--spacing) * 4);
  }

  .product-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius);
    padding: calc(var(--spacing) * 4);
    transition: var(--transition);
    position: relative;
    overflow: hidden;
  }

  .product-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--primary), var(--secondary));
    transform: scaleX(0);
    transition: var(--transition);
  }

  .product-card:hover::before {
    transform: scaleX(1);
  }

  .product-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-hover);
  }

  .product-icon {
    margin-bottom: calc(var(--spacing) * 3);
  }

  .product-title {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: calc(var(--spacing) * 2);
    color: var(--light);
  }

  .product-description {
    color: var(--gray);
    line-height: 1.6;
    margin-bottom: calc(var(--spacing) * 3);
  }

  .product-features {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .product-features li {
    color: var(--gray);
    margin-bottom: calc(var(--spacing));
    padding-left: calc(var(--spacing) * 3);
    position: relative;
  }

  .product-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--secondary);
    font-weight: bold;
  }

  /* About Section */
  .about {
    padding: calc(var(--spacing) * 12) 0;
    background: var(--dark-light);
  }

  .about-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(var(--spacing) * 8);
    align-items: center;
  }

  .about-description {
    font-size: 1.125rem;
    color: var(--gray);
    line-height: 1.6;
    margin-bottom: calc(var(--spacing) * 4);
  }

  .about-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: calc(var(--spacing) * 3);
  }

  .stat {
    text-align: center;
  }

  .stat-number {
    font-size: 2rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: calc(var(--spacing));
  }

  .stat-label {
    color: var(--gray);
    font-size: 0.9rem;
  }

  .tech-stack {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: calc(var(--spacing) * 2);
  }

  .tech-item {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--border-radius);
    padding: calc(var(--spacing) * 2);
    text-align: center;
    color: var(--light);
    font-weight: 500;
    transition: var(--transition);
  }

  .tech-item:hover {
    background: rgba(0, 102, 204, 0.1);
    border-color: var(--primary);
    transform: translateY(-2px);
  }

  /* Contact Section */
  .contact {
    padding: calc(var(--spacing) * 12) 0;
    background: var(--dark-light);
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(var(--spacing) * 8);
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing) * 4);
  }

  .contact-item {
    display: flex;
    align-items: flex-start;
    gap: calc(var(--spacing) * 2);
  }

  .contact-icon {
    font-size: 1.5rem;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .contact-item h4 {
    color: var(--light);
    margin: 0 0 calc(var(--spacing)) 0;
    font-weight: 600;
  }

  .contact-item p {
    color: var(--gray);
    margin: 0;
    line-height: 1.5;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: calc(var(--spacing) * 3);
  }

  .form-group {
    position: relative;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: calc(var(--spacing) * 2);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: var(--border-radius);
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    color: var(--light);
    font-family: "Roboto", sans-serif;
    transition: var(--transition);
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 3px rgba(0, 102, 204, 0.1);
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: var(--gray);
  }

  /* Footer */
  .footer {
    background: var(--dark);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding: calc(var(--spacing) * 8) 0 calc(var(--spacing) * 3) 0;
  }

  .footer-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(var(--spacing) * 8);
    margin-bottom: calc(var(--spacing) * 4);
  }

  .footer-brand p {
    color: var(--gray);
    margin-top: calc(var(--spacing) * 2);
  }

  .footer-links {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: calc(var(--spacing) * 4);
  }

  .footer-column h4 {
    color: var(--light);
    margin-bottom: calc(var(--spacing) * 2);
    font-weight: 600;
  }

  .footer-column a {
    display: block;
    color: var(--gray);
    text-decoration: none;
    margin-bottom: calc(var(--spacing));
    transition: var(--transition);
  }

  .footer-column a:hover {
    color: var(--primary);
  }

  .footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: calc(var(--spacing) * 3);
    text-align: center;
    color: var(--gray);
  }

  /* Animations */
  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .hero-container,
    .about-content,
    .contact-content {
      grid-template-columns: 1fr;
      gap: calc(var(--spacing) * 4);
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .tech-stack {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .nav-menu {
      display: none;
    }

    .nav-toggle {
      display: flex;
    }

    .hero-title {
      font-size: 2rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .about-stats {
      grid-template-columns: 1fr;
    }

    .footer-content {
      grid-template-columns: 1fr;
    }

    .footer-links {
      grid-template-columns: 1fr;
    }

    .hero-cta {
      flex-direction: column;
    }

    .btn {
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .hero-visual {
      height: 300px;
    }

    .gradient-orb {
      width: 200px;
      height: 200px;
    }

    .gradient-mesh {
      width: 250px;
      height: 250px;
    }
  }
</style>

<script>
  // Smooth scrolling for navigation links
  document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener("click", function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute("href"));
      if (target) {
        target.scrollIntoView({
          behavior: "smooth",
          block: "start",
        });
      }
    });
  });

  // Form submission
  document
    .querySelector(".contact-form")
    .addEventListener("submit", function (e) {
      e.preventDefault();

      // Simple form validation and submission simulation
      const formData = new FormData(this);

      // Simulate form submission
      const button = this.querySelector('button[type="submit"]');
      const originalText = button.textContent;

      button.textContent = "Enviando...";
      button.disabled = true;

      setTimeout(() => {
        alert(
          "¡Gracias por tu consulta! Nos pondremos en contacto contigo pronto."
        );
        this.reset();
        button.textContent = originalText;
        button.disabled = false;
      }, 1000);
    });

  // Add scroll effect to navigation
  window.addEventListener("scroll", function () {
    const nav = document.querySelector(".nav");
    if (window.scrollY > 50) {
      nav.style.background = "rgba(10, 10, 10, 0.98)";
    } else {
      nav.style.background = "rgba(10, 10, 10, 0.95)";
    }
  });
</script>
